modules = ["nodejs-24"]

[deployment]
router = "application"
deploymentTarget = "static"
build = ["bash", "-c", "pnpm --filter @workspace/smartwod-timer run build"]
publicDir = "artifacts/smartwod-timer/dist/public"

[deployment.postBuild]
args = ["pnpm", "store", "prune"]
env = { "CI" = "true" }

[workflows]
runButton = "Project"

[[workflows.workflow]]
name = "Project"
mode = "parallel"
author = "agent"

[[workflows.workflow.tasks]]
task = "workflow.run"
args = "Start application"

[[workflows.workflow]]
name = "Start application"
author = "agent"

[[workflows.workflow.tasks]]
task = "shell.exec"
args = "PORT=5000 BASE_PATH=/ pnpm --filter @workspace/smartwod-timer run dev & node -e \"const h=require('http');h.createServer((req,res)=>{res.writeHead(307,{'Location':'http://localhost:5000'+req.url});res.end()}).listen(22961,()=>console.log('Redirect on 22961'))\""
waitForPort = 5000

[workflows.workflow.metadata]
outputType = "webview"

[agent]
stack = "BEST_EFFORT_FALLBACK"
expertMode = true

[postMerge]
path = "scripts/post-merge.sh"
timeoutMs = 20000

[[ports]]
localPort = 5000
externalPort = 80

[nix]
channel = "stable-25_05"
